# install jedi vim 
sudo apt-get install vim-python-jedi
pip install jedi

#copy config file
#setup plugins
# pathogen
mkdir -p ~/.vim/autoload ~/.vim/bundle && \
curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim
# ag solver searcher
sudo apt-get install silversearcher-ag
cd ~/.vim/bundle && git clone https://github.com/rking/ag.vim ag
# install supertab
cd .vim/bundles && git clone https://github.com/ervandew/supertab.git
# need to make sure python support is available in vim..compile vim


